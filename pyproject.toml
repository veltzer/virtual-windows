[project]
name = "virtual-windows"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "mypy",
    "pylint",
    "pytest",
    "ruff",
    "yamllint",
]

[tool.uv]
# Scripts repo: not an installable package.
package = false
